<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    android:orientation="vertical" >
    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:id="@+id/info_ordine"
        android:layout_alignTop="@+id/ordine_completo"
        android:width="30dp"
        android:textColor="#ffffff"
        android:background="#082139"
        />
    <ListView
        android:layout_marginTop="30dp"
        android:id="@+id/ordine_completo"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:dividerHeight="1dp"
        />

    <Button
        android:id="@+id/conferma"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignBottom="@+id/ordine_completo"
        android:layout_gravity="center_horizontal"
        android:text="@string/conferma_ordine"
        android:textStyle="bold|italic"
        android:textColor="#3d5afe"
        android:typeface="serif"
        android:background="#ffffff"
        />






</RelativeLayout>